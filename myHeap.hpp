#ifndef MYHEAP_HPP
#define MYHEAP_HPP

#include <algorithm>
#include <functional>
#include <iostream>
#include <queue>
#include <vector>

void heapMedian(const std::vector<int> *instructions);

#endif
